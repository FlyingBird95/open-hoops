[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "open-hoops-dashboard-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "open-hoops",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg2-binary>=2.9",
    "celery[redis]>=5.4",
    "python-multipart>=0.0.9",
    "pydantic>=2.0",
    "pydantic-settings>=2.3",
]

[dependency-groups]
dev = ["pytest>=8.0", "httpx>=0.27", "open-hoops-testhelpers"]

[tool.uv.sources]
open-hoops = { workspace = true }
open-hoops-testhelpers = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
